#include <iostream>
#include "Matrix.h"

using namespace std;

int main(int argc, char** argv){

	Matrix data{};
	data.printMatrix();

	// initMatrix(matrix, rows, cols);
	// printMatrix(matrix, rows, cols);
	
	return 0;
}

